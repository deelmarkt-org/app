{
  "name": "deelmarkt-og-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for dynamic OG meta tags and crawler pre-rendering",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
